<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- View fbr.reason View Tree -->
    <record id="view_fbr_reason_tree" model="ir.ui.view">
        <field name="name">view.fbr.reason.tree</field>
        <field name="model">fbr.reason</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
            </tree>
        </field>
    </record>

    <!-- View fbr.reason form -->
    <record id="view_fbr_reason_form" model="ir.ui.view">
        <field name="name">view.fbr.reason.form</field>
        <field name="model">fbr.reason</field>
        <field name="arch" type="xml">
            <form string="Fbr Reason">
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="Name..."/>
                        </h1>
                    </div>
                </sheet>
            </form>
        </field>
    </record>

    <!-- View fbr.reason search -->
    <record id="view_fbr_reason_search" model="ir.ui.view">
        <field name="name">view.fbr.reason.search</field>
        <field name="model">fbr.reason</field>
        <field name="arch" type="xml">
            <search>
                <group expand="1" string="Group By">
                    <filter string="Name" name="name" domain="[]" context="{'group_by':'name'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action fbr.reason -->
    <record id="action_fbr_reason" model="ir.actions.act_window">
        <field name="name">Fbr Reason</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">fbr.reason</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                There is no examples click here to add new Fbr Reason.
            </p>
        </field>
    </record>

</odoo>
