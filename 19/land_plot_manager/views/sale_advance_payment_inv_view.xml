<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_sale_advance_payment_inv_form_inherit_custom" model="ir.ui.view">
        <field name="name">sale.advance.payment.inv.form.inherit.custom</field>
        <field name="model">sale.advance.payment.inv</field>
        <field name="inherit_id" ref="sale.view_sale_advance_payment_inv"/>
        <field name="arch" type="xml">

            <!-- Show Invoice Summary -->
            <xpath expr="//group" position="before">
                <group string="Invoice Summary" colspan="2">
                    <field name="already_invoiced_amount" readonly="1"/>
                    <field name="remaining_amount" readonly="1"/>
                </group>
            </xpath>

            <!-- Replace the advance_payment_method field -->
            <xpath expr="//field[@name='advance_payment_method']" position="replace">
                <group>
                    <!-- Hidden fields to trigger selection refresh -->
                    <field name="sale_order_id" invisible="1"/>
                    <field name="plan_type_for_selection" invisible="1"/>
                    
                    <!-- Custom payment method - dynamically shows options based on plan_type -->
                    <!-- Selection options are controlled by _get_custom_method_selection method -->
                    <field name="custom_method" 
                           widget="radio" 
                           required="1"
                           invisible="not sale_order_id"
                           options="{'no_create': True, 'no_create_edit': True}"/>

                    <!-- Custom amount field -->
                    <field name="custom_amount"
                           string="Payment Amount"
                           placeholder="Enter amount"
                           invisible="not custom_method"/>

                    <!-- PAL Number: 
                         - Required for Regular (always)
                         - Required for Possession (standard plans only: full/installment)
                         - Required for Installment (Navy plans only) -->
                    <field name="pal_number"
                           string="PAL Number"
                           placeholder="Enter PAL Number"
                           required="custom_method == 'regular' or (custom_method == 'possession' and sale_order_id.plan_type in ['full', 'installment']) or (custom_method == 'installment' and sale_order_id.plan_type in ['full_navy_1', 'full_navy_2', 'installment_navy_1', 'installment_navy_2'])"
                           invisible="custom_method not in ['regular', 'possession', 'installment'] or (custom_method == 'possession' and sale_order_id.plan_type not in ['full', 'installment']) or (custom_method == 'installment' and sale_order_id.plan_type not in ['full_navy_1', 'full_navy_2', 'installment_navy_1', 'installment_navy_2'])"/>

                </group>
            </xpath>

        </field>
    </record>
    
</odoo>
