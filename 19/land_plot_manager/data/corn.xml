<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <record id="ir_cron_post_due_invoices" model="ir.cron">
            <field name="name">Post due customer invoices (auto)</field>
            <field name="model_id" ref="model_sale_advance_payment_inv"/>
            <field name="state">code</field>
            <field name="code">model.cron_post_due_invoices()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active">True</field>
            <field name="nextcall" eval="(DateTime.now()).strftime('%Y-%m-%d %H:%M:%S')"/>
        </record>
    </data>
</odoo>
